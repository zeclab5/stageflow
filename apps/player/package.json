{
  "name": "@stageflow/player",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json && electron .",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^33.0.0",
    "fluent-ffmpeg": "^2.1.3"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg": "^2.1.3",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
